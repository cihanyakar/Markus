<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:services="using:Markus.Services"
        mc:Ignorable="d"
        x:Class="Markus.Views.AboutWindow"
        Title="About Markus"
        Width="440" Height="520"
        CanResize="False"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner"
        ExtendClientAreaToDecorationsHint="True"
        Background="Transparent"
        TransparencyLevelHint="AcrylicBlur">

  <Window.Styles>
    <!-- Card uses a softer corner and tighter padding so it reads more
         like a printed colophon than a chunky dialog. -->
    <Style Selector="Border.card">
      <Setter Property="Background" Value="{DynamicResource OverlayBg}"/>
      <Setter Property="BorderBrush" Value="{DynamicResource OverlayBorder}"/>
      <Setter Property="BorderThickness" Value="1"/>
      <Setter Property="CornerRadius" Value="14"/>
      <Setter Property="Padding" Value="32,32,32,22"/>
    </Style>
    <!-- Display: same condensed-light treatment used by the Welcome
         masthead, so the brand reads consistently across both surfaces. -->
    <Style Selector="TextBlock.display">
      <Setter Property="FontFamily" Value="-apple-system,BlinkMacSystemFont,Segoe UI,ui-sans-serif,system-ui,sans-serif"/>
      <Setter Property="FontWeight" Value="Light"/>
      <Setter Property="FontStretch" Value="Condensed"/>
      <Setter Property="FontSize" Value="38"/>
      <Setter Property="LetterSpacing" Value="-1.0"/>
      <Setter Property="Foreground" Value="{DynamicResource TextHigh}"/>
      <Setter Property="HorizontalAlignment" Value="Center"/>
    </Style>
    <Style Selector="TextBlock.version">
      <Setter Property="FontFamily" Value="Menlo,Consolas,ui-monospace,monospace"/>
      <Setter Property="FontSize" Value="11"/>
      <Setter Property="LetterSpacing" Value="0.4"/>
      <Setter Property="Foreground" Value="{DynamicResource TextLow}"/>
      <Setter Property="HorizontalAlignment" Value="Center"/>
    </Style>
    <Style Selector="TextBlock.tagline">
      <Setter Property="FontFamily" Value="-apple-system,BlinkMacSystemFont,Segoe UI,ui-sans-serif,system-ui,sans-serif"/>
      <Setter Property="FontSize" Value="13"/>
      <Setter Property="Foreground" Value="{DynamicResource TextMedium}"/>
      <Setter Property="HorizontalAlignment" Value="Center"/>
      <Setter Property="TextAlignment" Value="Center"/>
      <Setter Property="TextWrapping" Value="Wrap"/>
    </Style>
    <Style Selector="TextBlock.copyright">
      <Setter Property="FontFamily" Value="-apple-system,BlinkMacSystemFont,Segoe UI,ui-sans-serif,system-ui,sans-serif"/>
      <Setter Property="FontSize" Value="11"/>
      <Setter Property="Foreground" Value="{DynamicResource TextLow}"/>
      <Setter Property="HorizontalAlignment" Value="Center"/>
      <Setter Property="TextAlignment" Value="Center"/>
      <Setter Property="TextWrapping" Value="Wrap"/>
    </Style>
    <Style Selector="Button.link">
      <Setter Property="Background" Value="Transparent"/>
      <Setter Property="BorderThickness" Value="0"/>
      <Setter Property="Padding" Value="10,6"/>
      <Setter Property="Foreground" Value="{DynamicResource Accent}"/>
      <Setter Property="FontFamily" Value="-apple-system,BlinkMacSystemFont,Segoe UI,ui-sans-serif,system-ui,sans-serif"/>
      <Setter Property="FontSize" Value="12"/>
      <Setter Property="FontWeight" Value="Medium"/>
      <Setter Property="Cursor" Value="Hand"/>
    </Style>
    <Style Selector="Button.link:pointerover /template/ ContentPresenter">
      <Setter Property="Background" Value="Transparent"/>
      <Setter Property="TextBlock.TextDecorations" Value="Underline"/>
    </Style>
    <Style Selector="Button.link:pressed /template/ ContentPresenter">
      <Setter Property="Background" Value="Transparent"/>
    </Style>
  </Window.Styles>

  <Panel>

    <!-- Atmosphere mesh, lighter than the main window. -->
    <Panel ZIndex="-1">
      <Rectangle IsHitTestVisible="False" Opacity="0.55">
        <Rectangle.Fill>
          <RadialGradientBrush Center="20%,14%" RadiusX="0.65" RadiusY="0.7">
            <GradientStop Color="{DynamicResource MeshAColor}" Offset="0"/>
            <GradientStop Color="#00FFFFFF" Offset="1"/>
          </RadialGradientBrush>
        </Rectangle.Fill>
      </Rectangle>
      <Rectangle IsHitTestVisible="False" Opacity="0.45">
        <Rectangle.Fill>
          <RadialGradientBrush Center="84%,86%" RadiusX="0.6" RadiusY="0.65">
            <GradientStop Color="{DynamicResource MeshBColor}" Offset="0"/>
            <GradientStop Color="#00FFFFFF" Offset="1"/>
          </RadialGradientBrush>
        </Rectangle.Fill>
      </Rectangle>
    </Panel>

    <Border Classes="card"
            Margin="22"
            VerticalAlignment="Stretch"
            HorizontalAlignment="Stretch"
            PointerPressed="OnCardPointerPressed">
      <Grid RowDefinitions="*,Auto">

        <StackPanel Grid.Row="0"
                    Spacing="14"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Stretch">

          <Image Name="AppIconImage"
                 Width="96" Height="96"
                 HorizontalAlignment="Center"
                 Stretch="Uniform"/>

          <!-- Eyebrow: sienna ember + caption set above the wordmark so
               About reads as a small colophon page rather than a generic
               splash. Mirrors the Welcome view's masthead. -->
          <StackPanel Orientation="Horizontal"
                      HorizontalAlignment="Center"
                      Spacing="8"
                      Margin="0,4,0,0">
            <Ellipse Width="6" Height="6"
                     Fill="{DynamicResource Accent}"
                     VerticalAlignment="Center"
                     Opacity="0.9"/>
            <TextBlock Classes="section"
                       Text="COLOPHON"
                       VerticalAlignment="Center"/>
          </StackPanel>

          <StackPanel Spacing="6">
            <TextBlock Classes="display" Text="Markus"/>
            <TextBlock Classes="version"
                       Name="VersionLabel"
                       Text="dev"/>
          </StackPanel>

          <Border Height="1"
                  Width="56"
                  Background="{DynamicResource Accent}"
                  Opacity="0.5"
                  HorizontalAlignment="Center"
                  Margin="0,4,0,4"/>

          <TextBlock Classes="tagline"
                     Margin="0,2,0,0"
                     Text="A fast, elegant Markdown editor for macOS, Windows and Linux. .NET 11 · NativeAOT."/>

          <StackPanel Orientation="Horizontal"
                      HorizontalAlignment="Center"
                      Spacing="4"
                      Margin="0,6,0,0">
            <Button Classes="link"
                    Content="GitHub"
                    Click="OnGithubClick"/>
            <TextBlock VerticalAlignment="Center"
                       Foreground="{DynamicResource TextLow}"
                       FontSize="12"
                       Text="·"/>
            <Button Classes="link"
                    Content="License"
                    Click="OnLicenseClick"/>
            <TextBlock VerticalAlignment="Center"
                       Foreground="{DynamicResource TextLow}"
                       FontSize="12"
                       Text="·"/>
            <Button Classes="link"
                    Content="Check for Updates"
                    Command="{x:Static services:AppCommands.CheckForUpdates}"/>
          </StackPanel>

          <TextBlock Classes="copyright"
                     Margin="0,8,0,0"
                     Text="© 2026 Cihan Yakar. MIT License."/>

        </StackPanel>

        <Grid Grid.Row="1" ColumnDefinitions="*,Auto" Margin="0,12,0,0">
          <Button Grid.Column="1"
                  Content="Close"
                  MinWidth="84"
                  Click="OnCloseClick"
                  IsDefault="True"
                  IsCancel="True"/>
        </Grid>

      </Grid>
    </Border>

  </Panel>
</Window>
